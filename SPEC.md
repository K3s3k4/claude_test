# 仕様書 — claude-projects

最終更新: 2026-09-15

## 1. 概要

React製のWebアプリ「回収の羅針盤」。2つのデータ源を使い分ける。

- **netkeiba**: 個別レースをその場でスクレイピングし、独自スコアリングで予想・推奨買い目を出す
  （`/predict`＝netkeiba情報取得）。ライブ・少量アクセス向け。
- **JRDB**: 有料会員データを日次で一括ダウンロードしてローカルに蓄積し（現在数年分・1万レース超）、
  総合指数ベースの推定勝率・確度スコア・推奨買い目を全レースに機械的に適用してバックテストする
  （`/jrdb`・ダッシュボード・予測履歴）。大量データでの検証・分析が主目的。

netkeibaは度々アクセス制限（HTTP 400ブロック）に遭うため、量が必要な分析・バックテストはJRDBに全面移行した。

## 2. 構成

```
claude-projects/
├── src/                      # フロントエンド (React + TypeScript, Vite)
│   ├── App.tsx                 # ルーティング定義 (react-router-dom)
│   ├── components/
│   │   ├── AppNavbar.tsx       # ナビゲーションバー(PC) + NAV_ITEMS定義
│   │   ├── MobileBottomNav.tsx # スマホ用ボトムタブ(768px未満)
│   │   ├── Dashboard.tsx       # ダッシュボード (JRDB直近ピック・回収率推移・同期ボタン)
│   │   ├── JrdbSearch.tsx      # JRDB検索(日付・場・レース番号指定で出走馬/指数/買い目/結果)
│   │   ├── BetTypeStatsCard.tsx# 券種別的中率・回収率カード(期間・確信度フィルタ、Dashboard/History共用)
│   │   ├── Prediction.tsx      # netkeiba情報取得(旧・競馬予想)画面
│   │   └── History.tsx         # 予測履歴(JRDB検索一覧、デフォルト直近3週間)
│   └── main.tsx                 # エントリポイント (Bootstrap CSS/JS読み込み)
├── server/                   # バックエンド (Express + TypeScript, tsx実行)
│   ├── index.ts                 # APIサーバー本体(netkeiba系+JRDB系ルート、週次自動同期)
│   ├── netkeiba.ts              # netkeibaスクレイピング処理
│   ├── predict.ts               # netkeiba予想のスコアリング・買い目生成ロジック
│   ├── probability.ts           # 確率論・数理モデル(softmax/Harville法/経験ベイズ/EV)
│   ├── db.ts                    # SQLite永続化(netkeiba予想・買い目・結果の保存/照合/集計)
│   ├── jrdb.ts                  # JRDB認証ダウンロード(LZH解凍)・同期ジョブ
│   └── jrdbParser.ts            # JRDB固定長データのパース・分析・バックテストエンジン
├── scripts/                  # 使い捨て解析・一括取得スクリプト(6節参照)
├── data/
│   ├── predictions.db          # netkeiba予想のSQLite(gitignore対象)
│   └── jrdb/                   # JRDBダウンロード済み生データ(gitignore対象)
│       ├── Kyi/ Sed/ Hjc/ Ukc/  # 種別ごとのYYMMDD.txtファイル
│       └── .last-sync           # 週次自動同期の最終実行日時
├── .env                      # JRDB_USER/JRDB_PASSWORD (gitignore対象、dotenvで読み込み)
├── vite.config.ts             # /api を localhost:3001 へプロキシ。host:true・polling監視
└── package.json
```

### 技術スタック

| 分類 | 技術 |
|---|---|
| フロントエンド | React 18, TypeScript, Vite 5, react-router-dom, Bootstrap 5, Bootstrap Icons, recharts |
| バックエンド | Express 5, TypeScript (tsx watch, nodemon --legacy-watch), axios, cheerio, iconv-lite, better-sqlite3, dotenv |
| JRDBデータ解凍 | `lhasa`(LZH展開、要`sudo apt-get install lhasa`) |
| 実行環境 | Node.js 24 (LTS)、WSL2(Windows) |

### 開発環境の注意

- プロジェクトが`/mnt/c`(WindowsドライブOneDrive配下)にあるため、WSL2のinotifyがファイル変更を検知できない。
  Vite/API共にポーリング監視(`usePolling`/`nodemon --legacy-watch`)に切り替え済み。
- `.env`はdotenvで`server/index.ts`冒頭にて読み込む。以前は開発サーバー起動時にシェルへ手動で
  `source .env`していないとJRDB認証が全件失敗するバグがあったため、これを廃してコード側で保証している。

## 3. 画面

### 3.1 ダッシュボード (`/`)

- **JRDBデータの更新**: 未取得分(過去14日・未来10日)をボタン一つで取得。ジョブの進捗をポーリング表示
- **直近の自信がある買い目(JRDB)**: 直近で入手できている開催日(未来開催日が既に公開されていればそちら優先)のうち、
  確度スコア上位4件を買い目・金額つきで表示
- **券種別 的中率・回収率(JRDBバックテスト)**: `BetTypeStatsCard`。期間(全期間/1年/3ヶ月/30日/7日)・確信度で絞り込み
- **回収率の推移・累積回収率の推移**: JRDBアーカイブ全体を日別/月別に集計したグラフ(recharts)

### 3.2 JRDB検索 (`/jrdb`)

1. 開催日を選択 → その日にダウンロード済みの開催場・レース番号がボタンで一覧表示(確信度で色分け)
2. レースを選ぶと、出走馬全頭をIDM・総合指数・騎手指数・基準オッズ等つきで表示
3. softmaxで算出した推定勝率・確度スコア・ファクトベースの分析文(リード文)
4. 推奨買い目(単勝〜三連単、予算配分つき)
5. 結果確定済みなら実際の着順・全券種の的中/不的中・払戻・レース単体の回収率も表示

### 3.3 netkeiba情報取得 (`/predict`)

netkeibaのrace_idを入力→出走馬の過去成績・血統を取得してスコアリング。推奨買い目(信頼度・予算配分つき)、
予想結果テーブル(各行「詳細」でスコア内訳・3世代血統を展開)。結果はSQLiteへ自動保存。

### 3.4 予測履歴 (`/history`)

- `BetTypeStatsCard`(3.1と共通)
- **予測したレース一覧**: JRDBアーカイブを検索。デフォルト直近3週間、期間・確信度で絞り込み可能。
  回収率は一覧に最初から数値表示。行クリックで着順・買い目別の的中/不的中・払戻を展開

## 4. JRDBデータパイプライン

### 4.1 認証・ダウンロード (`server/jrdb.ts`)

- `https://jrdb.com/member/data/{種別}/{PREFIX}{yymmdd}.lzh` にHTTP Basic認証(`JRDB_USER`/`JRDB_PASSWORD`)でアクセス
- 200なら取得しLZH解凍(`lhasa`をchild_processで実行。`xfq2w=<dir>`のように**モード+オプションを1トークンに連結**しないと解釈されない点に注意)、404ならその日データなし(スキップ)、401は`JrdbAuthError`
- 既に展開済みのファイルはネットワークアクセスせずスキップ(再実行で続きから進む)
- 実地検証で**約27年分(1999年〜)**遡って取得可能なことを確認済み

### 4.2 対応データ種別

| 種別 | ファイル | レコード長 | 内容 |
|---|---|---|---|
| Kyi | 競走馬データ | 1024byte | 出走馬ごとの指数群(IDM・騎手指数・情報指数・調教指数・厩舎指数・総合指数・展開予想指数等)、基準オッズ・人気、枠番、血統登録番号 |
| Sed | 成績データ | 376byte | 着順・タイム・確定オッズ・確定人気・単勝/複勝払戻・レース名/グレード |
| Hjc | 払戻情報データ | 444byte(レース単位) | 単勝〜三連単、全券種の確定配当(馬番組合せ+払戻金) |
| Ukc | 馬基本データ | 292byte | 父馬名・母馬名・母父馬名・系統コード(血統登録番号でKyi/Sedと結合) |

各ファイルはJRDB公式仕様書(`https://jrdb.com/program/{種別}/xxx_doc.txt`)のバイト位置に基づき、
`server/jrdbParser.ts`内で固定長バイト列から直接デコードしている(Shift-JIS、iconv-lite使用)。

### 4.3 同期・自動更新

- `syncJrdbData()`: 過去14日・未来10日分のKyi/Sed/Hjcの未取得分を埋める(既存ファイルはスキップ)
- サーバー起動時、および24時間おきのチェックで**最後の同期から7日以上経っていれば自動実行**(`data/jrdb/.last-sync`で判定)
- ダッシュボードの「JRDBデータの更新」ボタンから手動実行も可能(`POST /api/jrdb/sync` + ポーリング)
- 新規データがダウンロードされた場合、バックテストキャッシュを破棄し再計算(4.4節)

### 4.4 分析エンジン (`server/jrdbParser.ts`)

- `analyzeJrdbRace`: 総合指数を`softmaxProbabilities`(温度=8、後述のキャリブレーションで調整済み)にかけ推定勝率化。
  1位・2位の勝率差から確度スコア(0-100、`confidenceScore`)とラベル(堅い/やや堅い/混戦)を算出。
  ファクトベースの分析文(本命馬の強み・弱みを出走頭数内の順位で表現)を生成
- `suggestJrdbBets`: 確度ラベルに応じたBOXサイズ(3/4/5頭)で単勝〜三連単を生成し、予算配分(既定3,000円、
  券種均等×確率比例、100円単位)で金額を付与
- `computeJrdbActualReturn`: 提案した買い目をSED(単勝・複勝払戻)・HJC(全券種払戻)と突き合わせ、
  的中/不的中・実際の払戻・回収率を計算
- `computeJrdbBacktestStats` / `computeJrdbBacktestTimeseries`: アーカイブ全体(または期間・確信度で絞り込み)を
  集計して券種別・期間別の的中率・回収率を返す。10年分(3万件超)を毎回生ファイルから読み直すと非常に重く、
  以前は起動時のキャッシュ温め処理(3系統が独立にフルスキャン)が**JSヒープ不足でサーバーをクラッシュさせていた**。
  現在は「日付ごとの券種別集計」という小さい結果だけを`data/jrdb-analysis-cache.json`に永続化し、
  新しく追加された日付だけを差分計算する方式(`ensureAnalysisCache`)に変更済み。
  サーバー再起動時も、新規ダウンロード分の日付だけスキャンすれば済む(例: 1,085日分の初回構築後は
  再起動のたびに数日分の差分計算のみ)。ファイル読み込みは`SCAN_CONCURRENCY`(16件)ずつバッチ処理し、
  ピークメモリを抑えている。さらに10分TTLのインメモリキャッシュ(`backtestCache`/`timeseriesCache`)を重ねている
- `searchJrdbRaces`: 予測履歴ページ用の検索可能なレース一覧

## 5. モデルの検証・キャリブレーション(バックテスト結果)

`scripts/jrdb-*.ts`で実際のアーカイブ(10年分バックフィル後は34,566レース、2016-2026年)を使って検証した内容。

| 検証項目 | 結果 |
|---|---|
| 全体の通算回収率(現行モデル) | **77.9%**(温度キャリブレーション後。理論上の控除率75-80%にほぼ一致) |
| softmax温度パラメータ | 予測勝率と実際の勝率をビン分けして比較(キャリブレーション)した結果、**温度8**が最良(誤差2pt未満)。デフォルト10から変更済み |
| 券種別回収率 | 単勝81.4%・複勝82.9%・馬連77.8%・ワイド78.9%・馬単77.4%・三連複72%・三連単69.3%(分散が大きい券種ほど低い) |
| 確信度フィルタ単体の効果 | ほぼ無効(76.9〜78.1%で横ばい)。確度スコアと実際の回収率相関は弱い |
| EV(推定勝率×オッズ)ベースの選定 | 温度10では悪化(69-74%)、温度8補正後は改善(EV>1.5で80.5%)するが現行超えには未到達 |
| 市場オッズとの乖離(バリューベット、単勝) | **10年分全アーカイブ(splitなし、試行537〜230,660件)で最終確認**: エッジ+8pt〜+20ptの範囲で回収率80.7〜84%のプラトーを形成(現行ベースライン74〜79%比+5〜10pt)。エッジ+25pt以上はサンプルが1,000件台に減るとともに78〜81%まで低下し、「エッジが大きいほど回収率が上がる」わけではないと判明(下記「結論」参照) |
| 市場オッズとの乖離(バリューベット、複勝) | **10年分データ(試行4,607件)で再検証しエッジ+35pt以上で88.9%を確認**(3年分データの89.4%・試行1,518件とほぼ一致)。数千件規模で再現したため**現時点で最も信頼できる単体戦略**。エッジ+20pt〜+40ptの範囲で85〜89%の安定したプラトーを形成 |
| 枠番バイアス | **10年分データで再検証したところ結果が反転**: 3年分データでは「1枠77%〜2枠67.4%」だったが、10年分では1枠72.9%〜8枠72.5%で最大最小差が縮小し、特に2枠は67.4%→75%(最良側)へ逆転。**枠番バイアスは小サンプルのノイズであり、安定した傾向ではないと判明**。実運用への組み込みは非推奨 |
| 競馬場バイアス | **10年分データで再検証**: 福島76.9%・小倉75.6%は引き続きやや高めで一部再現したが、3年分データで最低だった札幌(67.4%)は10年分では72.5%まで回復し順位が変動。全場67〜77%のレンジに収束しており、**枠番バイアス同様、単体で頼れるほど強い・安定した信号ではない** |
| 三連単・単勝エッジ馬限定ボックス | 3年分データでは「エッジ+6pt以上で135.4%(試行152件・的中3件)」で要検証としていたが、**10年分データ(試行56,376〜103,698件)で再検証すると エッジ+0pt→79.9%→+2pt→147.8%→+4pt→63.3%→+6pt→65.4%と非単調に乱高下**。三連単は的中率0.2〜1.6%と極端に低く、少数の超高額配当が平均を大きく歪めるため、**回収率の平均値だけでは信頼できる指標にならない**(中央値や信頼区間を使った再評価が必要)。実運用への組み込みは非推奨 |
| ロジスティック回帰モデル(市場オッズ含む、+血統) | 単勝本命1点固定で的中率30.6%(現行29.8%から改善)だが回収率76.7%(現行79.9%から悪化)。的中率↑≠回収率↑の典型例 |
| ロジスティック回帰モデル(市場オッズ除外、JRDB指数+血統のみ) | 市場を模倣しているのが原因かと仮説を立てて検証したが、除外しても回収率75.1%とさらに悪化。「市場模倣」が原因ではなく、**単勝本命1点固定という賭け方自体が市場効率に対して構造的に不利**と判明 |
| **バリューベット×held-outテスト検証(単勝、総合指数vsモデル確率)** | 直近20%のtestデータ(学習未使用)で検証。**3年分データ(試行221件)では一時的にエッジ+16pt以上で回収率116%**という結果が出たが、**10年分データ(試行903件、約4倍のサンプル)で再検証すると89.4%まで低下**し、100%超えは再現しなかった。小サンプル特有のノイズだったと判明(下記「結論」参照) |
| **バリューベット閾値の頑健性(検証の重ね掛け)** | 直近20%のheld-outテストのみ(903〜43,997試行)では回収率75.8〜92.4%で**エッジが大きいほど改善**するように見えたが、**10年分全アーカイブ(splitなし、537〜230,660試行)で再確認すると80.7〜84%で頭打ちになり、上昇トレンドは再現しなかった**。held-outテストの「直近20%」もまた偶然良い期間を切り出していた可能性が高い。**全アーカイブでの80〜84%(エッジ+8pt〜+20pt)が現時点で最も信頼できる推定値** |

**結論**: 単純な「総合指数→softmax→本命」では市場平均(75-80%)に収束し、市場を出し抜く要素がない。
自作ロジスティック回帰モデルは的中率を改善しても回収率は改善しない(むしろ悪化する)ことが2パターン(市場込み/市場除外)で確認された。
市場オッズとの乖離(バリューベット)は現行を安定して上回る唯一の方向性だが、**3年分データで見えた「エッジ+16pt以上→回収率116%」は10年分データでは89.4%に収束し、100%超えは再現しなかった**——小サンプル(試行200件台)での高回収率は偶然のノイズであり得るという教訓。
一方で、**10年分全アーカイブという最大サンプルで、単勝エッジ+8pt〜+20ptが80〜84%、複勝エッジ+20pt〜+40ptが85〜89%(いずれもベースライン比+5〜10pt)を維持する**ことは確認できており、これが現時点で最も再現性のある改善策。ただし単勝側は「held-outテストの直近20%だけ」で見ると83〜92%まで上振れして見えたため、**必ず全アーカイブまたは複数期間のウォークフォワード検証で裏を取ってから数値を採用する**運用にしている。
枠番バイアス・競馬場バイアス・三連単エッジボックス・血統(母父馬)フィルタは、いずれも一部の検証(3年分データ、または単一train/testスプリット)では有望に見えたが、10年分全データやウォークフォワード検証で再確認するとことごとく再現しなかった(枠番・競馬場は傾向が反転、三連単は少数の超高額配当に平均が歪められる高分散な券種で非単調に乱高下、血統フィルタは4期間中2期間で悪化)——**小サンプル・単一splitのバックテストは、有望に見える結果でも大規模データや複数期間の検証で裏取りするまで信用しない**という方法論上の教訓が今回のセッション最大の収穫。
100%超えにはまだ届いていないため、複数の券種・条件を組み合わせた複合戦略を次の段階として検討する(ケリー基準などの資金配分最適化は、平均回収率が100%を超える戦略が見つかってから)。

**ケリー基準での資金配分実験(`jrdb-kelly-experiment.ts`)**: 単勝エッジ+8pt以上(試行14,789件、均等賭けの回収率81.1%)に対し、フルケリー〜クォーターケリー(上限3〜10%キャップ付き)の6パターンすべてで**初期資金10万円がほぼ0円まで溶けて破産**した。原因は、ケリー基準がそもそも「真に期待値プラス(平均回収率>100%)」の賭けにのみ適用できる手法であるのに対し、現状発見している戦略はいずれも平均回収率が100%未満(市場より相対的に良いだけで絶対的にはまだ負けている)だから。期待値マイナスの賭けにケリー式の複利レバレッジをかけると、損失が加速度的に拡大し破産に至る。**資金配分の最適化は、平均回収率が100%を明確に超える戦略が見つかってから初めて意味を持つ**——現時点では優先順位が逆で、まず複数シグナルの組み合わせ等で真のプラス期待値を作ることが先決、という教訓が得られた。

**血統フィルタとバリューベットの組み合わせ(`jrdb-value-bet-pedigree-experiment.ts`)**: 単勝バリューベット(エッジ+8pt以上)に母父馬の勝率フィルタを重ねると、単一のtrain/testスプリット(直近20%、試行2,925件)では回収率が85.4%→96.9%(母父馬勝率>=平均×1.2、試行319件)まで改善するように見えた。しかし**4期間のウォークフォワード検証**(各foldで直前までの全データを使って学習・独立した期間でテスト)を行うと、fold1で80.1%→74.2%(悪化)、fold2で79.7%→71.2%(悪化)、fold3で81.1%→85.7%(改善)、fold4で85.4%→92.1%(改善、これが単一split検証と同じ期間)と**4期間中2期間で悪化・2期間で改善**しており、一貫した効果は確認できなかった。単一splitで見えた好成績は直近の運の良い期間だけを見ていたことによる過大評価であり、**父馬・母父馬の勝率エンコーディング単体では、まだ実用に足る安定したフィルタとは言えない**という結論。枠番・競馬場バイアスと同様のパターンであり、「1回のtrain/testスプリットでは判断せず、複数の独立した期間でウォークフォワード検証する」ことの重要性を改めて示した。

**騎手・調教師の実勝率、血統×馬場適性フィルタ(`jrdb-jockey-trainer-pedigree-experiment.ts`)**: 前回の反省を踏まえ、最初から5分割ウォークフォワード検証で実施。
- **騎手・調教師の実勝率フィルタは効果なし**: 単勝バリューベット(エッジ+8pt以上、全fold合算ベースライン81.5%)に重ねても、騎手勝率>=平均×1.1〜1.2で78.1〜81.6%、調教師勝率>=平均×1.1〜1.2で79.7〜82.8%と、ベースラインとほぼ同等か下回る。市場がすでにこれらの情報を織り込んでいると考えられる。
- **父馬×馬場適性(芝/ダート別に父系の実績を条件付け)フィルタは有効性を確認**: 単純な父馬勝率(馬場を区別しない)ではなく、「その父馬の産駒が芝/ダートそれぞれでどれだけ勝っているか」を条件付けたところ、4fold中3foldで一貫して改善(fold1: 80.1%→88.6%、fold2: 79.7%→82.5%、fold3: 81.1%→78.6%[唯一の悪化、軽微]、fold4: 85.4%→92.2%)。**全fold合算で回収率81.5%→85.7%(勝率>=平均×1.1、試行3,875件)、87.3%(×1.2、試行2,580件)**——セッション中で血統関連としては初めてウォークフォワード検証を安定して通過した結果。閾値をさらに上げる(×1.3以上)とサンプルが数百件→数件まで急減し回収率も頭打ち・低下するため、**×1.1〜1.2が実用上の最適ゾーン**。単純な父馬勝率より、馬場適性で条件付けた方が明確に優れていた点が重要(=血統は「誰の子か」だけでなく「どんな条件で強いか」まで見て初めて市場に対する優位性が出る)。

**父馬×距離帯適性(`jrdb-distance-aptitude-experiment.ts`)/フィルタの組み合わせ(`jrdb-pedigree-combo-experiment.ts`)**:
- **父馬×距離帯(スプリント/マイル/中距離/長距離)フィルタも有効性を確認**: 5分割ウォークフォワード検証で、勝率>=平均×1.2にて4fold中3foldで改善(fold1: 80.1%→84.4%、fold2: 79.7%→82.4%、fold3: 81.1%→101.5%[初めて単体foldで100%超え]、fold4: 85.4%→84.4%[唯一の悪化、軽微])。**全fold合算で回収率81.5%→87.4%(試行2,036件)**——馬場適性フィルタ(87.3%)とほぼ同等の効果。×1.1では改善なし(81.5%のまま)。
- **馬場×距離帯を1つのキーに結合すると逆に悪化**: 父馬×馬場×距離帯という3要素を1つの組み合わせキーにすると、細分化しすぎてサンプルが薄くなり回収率79.1%(×1.1)・77%(×1.2)とベースラインを下回った。
- **馬場フィルタと距離帯フィルタを「それぞれ別々に構築し、両方を満たす馬だけ」に絞る(積集合)方式でも上乗せ効果は無し**: 単独では馬場87.3%・距離帯87.4%だが、積集合では**85.8%(試行1,434件、単独より低くサンプルも半減)**。馬場適性と距離適性は完全に独立したシグナルではなく(強い父系は両方に相関しやすい)、組み合わせても加算的には改善しないと判明。**現時点の結論: 馬場適性か距離適性のどちらか片方を単独で使うのが最善**(×1.1〜1.2倍、87%前後)。

**市場自身の確信度で絞り込む(`jrdb-market-confidence-experiment.ts`)**: ユーザー提案——「堅い」ラベルはJRDBの総合指数(JRDBの予測数値)から算出されているため、JRDBの予測を信頼しているに過ぎない。そこで「市場(オッズ)自体が固いと判断しているレース」(客観的事実であり、JRDBの予測ではない)を独自に定義し直して検証した。市場確信度 = 市場暗示確率(オッズ由来)における1位・2位の差。5分割ウォークフォワード検証・全fold合算:
| 市場gap閾値 | 試行数 | 回収率(市場favorite買い) |
|---|---|---|
| 0pt以上(全レース) | 27,653件 | 77.6% |
| 10pt以上 | 9,397件 | 77.2% |
| 20pt以上 | 1,428件 | 80.3% |
| 30pt以上 | 55件 | 96.4% |
| 30pt以上+JRDB指数エッジ0pt以上 | 29件 | **99.3%**(ほぼ100%) |
| 40pt以上 | 4件 | 100%超(サンプル過小) |

閾値を上げるほど回収率が綺麗に上昇する傾向が試行1,000件超の規模でも確認でき、市場自身の確信度は回収率と正の相関があるという説得力のある結果。ただし**gap>=30pt以上はfold別のばらつきが大きい**(fold1:82.9%・fold2:83.8%・fold3:130%・fold4:111%、各fold試行数はわずか7〜17件)ため、100%超えが安定して再現するとはまだ断定できない。gap>=20pt(試行1,428件、統計的に十分な規模)の78〜82%台は信頼できる改善だが100%には届いていない。**今後アーカイブがさらに増えるか、複数券種に広げてこの高確信度帯のサンプルを増やせるかが鍵**。

**フォワードテスト機能(`jrdb-august-pedigree-forward-test.ts`)**: 1つの切り口(血統=父馬×馬場適性)だけに絞った「分析ベース」を2026年7月末までのデータのみで構築し(8月分は一切使わずリーク防止)、2026年8月という完全に未知の1ヶ月にレース単位でぶつける新機能。結果:
- フィルタなし(単勝エッジ+8pt以上のみ、試行190件): 回収率**98.2%**(8月はたまたま好調な月)
- 血統フィルタ適用後(父馬×馬場適性>=平均×1.2、試行39件): 回収率**80.5%**(フィルタなしより低下)

1ヶ月・試行39件という小サンプルでは、ウォークフォワード検証で確認した効果(全体で87%)が必ずしも毎月現れるとは限らないという現実を示す結果。血統フィルタの長期的な有効性を否定するものではないが、**月単位の短期では運の影響が大きく振れる**ことを再確認した。今後、月次でこの機能を回して「今月は効いたか」を継続的にモニタリングする運用が考えられる。

**多世代血統(系統コード)の検証(`jrdb-keito-aptitude-experiment.ts`)**: ユーザー要望「血統分析において、三世代前までしっかり見て」を受けて調査。JRDBには個体名ベースで祖父母・曾祖父母まで遡れる専用ファイルは無い(登録馬データKTAを確認したが、これは出走履歴・騎手調教師情報などのレース登録データで血統ではなかった)。一方、UKCには**父系統コード・母父系統コード**という、JRDBが多世代の血統系譜を「一族」単位に分類したフィールドが既にダウンロード済みデータに含まれており、これまで未使用だったため検証した。
結果: **父馬個体名ベースの既存フィルタ(87.3%)より信頼性が低い**。父系統コード単体(×1.1)は全体87%(試行2,298件)と数字上は悪くないが、fold別では69.8%〜104%と激しく変動し、×1.2に絞るとfold3で該当0件になるなど不安定。母父系統×馬場(×1.1)が比較的安定していた(78.4%〜95.4%、全体83.1%)が、それでも個体名ベースの87.3%には及ばない。**系統コードは複数の父馬を大きな一族単位でまとめるため、個々の父馬が持つ実力差の情報が薄まりノイズが増える**と考えられる。結論: 現時点では父馬個体名×馬場適性(87.3%)が最良。

**UKCの1999年まで遡及と祖先チェーンの構築(`server/pedigree.ts`, `jrdb-pedigree-depth-probe.ts`)**: 「真に祖父母の個体名まで見たい」という要望を受け、UKCが1999年分まで取得可能であることを確認して2000年1月〜2016年9月を追加取得(1,777ファイル、失敗0)。血統索引は53,737頭→**125,504頭**に拡大。UKCは1頭につき父・母・母父しか持たないため、祖先は「父馬自身のUKCレコードを馬名で引く」自己参照チェーンで辿る(`buildAncestors`)。到達率の実測:

| 世代 | バックフィル前 | バックフィル後 |
|---|---|---|
| 1世代(父・母) | 100% | 100% |
| 2世代(祖父母4頭) | 平均1.5頭(母父のみ確実) | 平均3.3頭(**83%**) |
| 3世代(曾祖父母8頭) | **0%** | 平均4.1頭(**51%**) |
| 4世代(16頭) | 0% | 平均1.4頭(8%) |
| 5世代(32頭) | 0% | 平均0.2頭(1%) |

5世代まで到達する馬も存在するが、4〜5世代は欠損が大きい(1980〜90年代の馬や外国産種牡馬はJRDB未収録)。**5世代完全取得は原理的に不可能**。

**インブリード(血統クロス)の検証(`jrdb-inbreeding-experiment.ts`)**: 深い世代の祖先名を個別の特徴量にするとサンプルが分散する(系統コード・複合キーで2度失敗済み)ため、「同一祖先が系図の複数箇所に現れる=クロス」という少数パターンに圧縮して検証。サンデーサイレンス3×3・4×3など実在のクロスを正しく検出できた(検出率: 単勝バリューベット母集団の7.3%)。結果は**逆相関**:
- クロスあり: 回収率**74.5%**(試行1,040件) / クロスなし: **82.2%**(試行10,779件)
- サンデーサイレンスのクロス: **72.5%**(試行991件)と特に低い
- 濃いクロス(3世代以内に2回)は83.7%(試行275件)とわずかに上回るが、fold別では38.9%〜97.5%と極端に振れる

**インブリードは回収率にプラスに働かない**(むしろマイナス)。人気を集めやすい血統構成のため市場に織り込まれ過ぎている可能性がある。

**血統だけを判断基準にした場合(`jrdb-pedigree-only-monthly.ts`)**: JRDB指数も市場オッズも使わず、各レースで「父系の適性勝率が最高の馬」の単勝1点のみを買った場合の月次成績(2026年6月末までの469,992頭で学習):

| | 7月(288レース) | 8月(360レース) |
|---|---|---|
| 血統のみ・父馬×馬場適性 | 的中率12.5% / 回収率**48.6%** | 的中率11.9% / 回収率**84.1%** |
| 血統のみ・父馬×距離帯適性 | 的中率11.1% / 回収率**47.8%** | 的中率12.2% / 回収率**44.4%** |
| 参考: 市場1番人気 | 的中率32.3% / 回収率79.9% | 的中率33.6% / 回収率83.2% |

**血統単独では買い目の判断基準にならない**。的中率が約12%まで落ち(市場1番人気なら33%)、回収率も隣接する月で48.6%→84.1%と35ポイント振れる。8月の84.1%も単に1番人気を買うだけ(83.2%)と同水準にすぎない。**血統が有効なのはあくまで「JRDB指数のバリューベットで母集団を絞った上での追加フィルタ」としてであり、単独のシグナルではない**というのが一連の検証の結論。

## 5.1 最終オッズ(TYB)によるエッジの測り直し

**背景**: これまでの全検証はKYIの`基準オッズ`を「市場の総意」として扱ってきたが、仕様書で**KYIの作成時刻が金曜19:00/土曜20:00(前日)**と判明。実データで前日オッズと最終オッズを比較すると**平均48.7%変動**し、20%以上動く馬が66.9%に達する。そこでTYB(直前情報・発走約15分前)を10年分取得し、最終オッズ基準で測り直した(`jrdb-final-odds-value-bet.ts`、対象468,673頭)。

| エッジ閾値 | A) 前日オッズ基準(従来) | B) 最終オッズ基準 |
|---|---|---|
| +8pt | 80.9%(試行14,361) | 79.9%(試行15,667) |
| +12pt | 82.5% | 82.2% |
| **+16pt** | **83.7%** | **87.4%**(試行2,949) |
| +20pt | 82.8% | **87.5%**(試行1,488) |
| +25pt | 81.5% | **85.4%** |
| +30pt | 78.4% | **86.6%** |

**最終オッズ基準の方が高エッジ帯で明確に優れる**。従来手法は+16ptをピークに減衰(83.7%→78.4%)していたが、最終オッズ基準では**+16pt以降も85〜87%台を維持**する。前日オッズのノイズが除かれ、市場の真の総意との乖離を測れるようになったため。**今後のバリューベットは最終オッズ基準(エッジ+16pt以上)を採用する。**

**オッズの動き自体もシグナルになる**:
| 前日→最終の動き | 回収率 |
|---|---|
| 人気上昇(最終/前日 0.5〜0.8) | 78.2%(試行65,429) |
| ほぼ変化なし(0.8〜1.25) | 74.0%(試行157,833) |
| 人気下降(1.25〜2.0) | 73.9%(試行147,723) |
| **大きく人気下降(2.0以上)** | **59.8%**(試行84,310) |

オッズが2倍以上に流れた馬は回収率59.8%と壊滅的。**売れていない馬は買わない**という明確な除外基準になる。

**我々の指数は「間違っている」のではなく「早い」**:
| 前日エッジ+8pt以上の馬を分類 | 試行 | 的中率 | 回収率 |
|---|---|---|---|
| 市場が最後まで修正しなかった | 9,075 | 19.6% | 80.0% |
| 市場が発走までに修正した | 5,286 | **33.9%** | **82.6%** |

直感に反するが、発走までに市場が我々の評価に追いついた馬の方が的中率が大幅に高い(19.5%→33.9%)。つまりJRDB指数は市場より**早く正しい**。ただし日本の馬券は最終オッズで払い戻されるため「早く気づくこと」自体は収益にならず、最終オッズ時点でもなお乖離が残っている馬を選ぶ必要がある(=B)の手法)。

## 5.2 フィルタの重ね掛けは機能しない(`jrdb-combined-strategy.ts`)

最終オッズ基準のエッジを土台に、これまで有効とされたフィルタ(血統・オッズ変動・新規解析した万券指数/降級フラグ/ブリンカー)を重ねて100%超えを狙ったが、**安定した改善は一つも得られなかった**。5分割ウォークフォワードで検証。

土台をエッジ+12ptにした場合(土台81.4%、試行5,035):
| フィルタ | 試行 | 回収率 |
|---|---|---|
| 土台のみ | 5,035 | **81.4%** |
| + 父馬×馬場適性>=平均×1.2 | 881 | 75.1% |
| + 父馬×距離帯適性>=平均×1.2 | 638 | 80.5% |
| + オッズ2倍以上流れた馬を除外 | 4,548 | 79.1% |
| + 人気上昇馬のみ(最終/前日<0.8) | 322 | 68.8% |
| + 万券指数>=50 | 866 | 78.8% |

**12パターン全てが土台を下回った。**

土台をエッジ+16ptにすると一部が100%を超えるが、**信頼できない**:
| フィルタ | 試行 | 回収率 | fold別 |
|---|---|---|---|
| 土台のみ | 2,359 | 85.8% | 91.3 / 72.9 / 95.3 / 83.7% |
| + 父馬×馬場適性 | 213 | 100.4% | **175.7** / 76.3 / 96.8 / 67.6% |
| + 万券指数>=50 | 328 | 96.8% | 128.9 / 85.6 / 104 / 79.4% |

血統フィルタの100.4%はfold1の175.7%(1foldあたり約53件)だけが押し上げたもので、**土台を+12ptに変えると同じフィルタが75.1%(悪化)と真逆になる**。万券指数も同様に一貫しない(+12pt土台では78.8%で悪化)。本セッションで確立した「小サンプル・fold間の大変動は信用しない」基準に抵触する。

**重要な含意**: 血統フィルタ(父馬×馬場適性)は前日オッズを土台にしたとき87.3%と有効に見えていたが、最終オッズを土台にすると効果が消える(+12pt土台で75.1%)。**以前の血統フィルタの効果は、前日オッズという不正確な基準に起因する見かけ上のものだった可能性が高い。**

またオッズ変動による除外(大きく流れた馬を切る)も逆効果だった。単独で見れば2倍以上流れた馬は59.8%と壊滅的だが、エッジで絞った母集団から除外すると**高配当の穴馬が消え**、エッジ戦略の収益源を失うため。

**結論: 優位性は「JRDB総合指数と市場の最終価格の乖離」という単一シグナルに集約されており、フィルタを足すとノイズが増えるだけ。最終オッズ基準エッジ+16pt単独(全期間87.4%、ウォークフォワード85.8%)が現時点の最良。**

## 5.3 血統を「勝率」ではなく「回収率」で評価すると持続性が出る(`jrdb-sire-roi-persistence.ts`)

**問題意識**: 5.2節までの血統検証は全て**勝率**を指標にしており、最終オッズで測り直すと効果が消えた。勝率は市場が既に織り込んでいる情報のため優位性が出ない。実務の血統データ分析では「全馬を買うと回収率は控除率ぶんに収束するので、それを超える種牡馬は過小評価されている」という**回収率ベース**の考え方が使われている(パンダズ競馬「過小評価ファクターデータベース」等)。

**検証方法**: 学習期間の回収率で血統を5段階に分け、**テスト期間(未来)での実際の回収率**を測る。過去の高回収率が未来にも続くなら持続性あり、続かないなら過去のノイズ。480,091頭・1,085日を5分割してウォークフォワード。全馬買いのベースは**71〜74%**(fold別)。

| 切り口 | 最低20% | 最高20% | 差 | 判定 |
|---|---|---|---|---|
| **父馬×馬場(芝ダ)** | 60.9% | **77.0%** | **16.1pt** | 持続性あり(単調増加) |
| **父馬単独** | 64.7% | **78.7%** | **14.0pt** | 持続性あり(単調増加) |
| **父馬×馬場状態(良/稍重/重/不良)** | 68.2% | **81.0%** | **12.8pt** | 持続性あり・最高値 |
| 父馬×距離帯 | 67.2% | 77.9% | 10.7pt | 持続性あり |
| ニックス(父×母父) | 60.4% | 69.4% | 9.0pt | **非単調(中位78.2%が最高)で信用できず** |
| 母父馬 | 69.3% | 72.3% | 3.0pt | ノイズ |
| 父馬×競馬場 | 72.3% | 74.6% | 2.3pt | ノイズ |

**父馬・父馬×馬場・父馬×馬場状態は5段階が綺麗に単調増加**し、各段階の試行も2.7万〜10万件と十分。**過去に回収率が高かった血統は将来も高い**ことが実証された。

**重要な含意**: 勝率で測ると効果が消えるのに回収率で測ると持続性が出る。これは**「どの血統が強いか」は市場が織り込み済みだが、「どの血統が安く見られているか」にはまだズレが残っている**ことを意味する。血統分析の指標選択を根本的に誤っていた。

**特筆事項**:
- **父馬×馬場状態の最高20%が81.0%と全切り口中で最高**。道悪適性という血統理論が回収率ベースで実証された。この切り口は本セッションで初めて検証した。
- **ニックス(父×母父)は実務で最重要とされる手法だが持続性が無い**。母父馬単独も同様(3.0pt)。**母系の情報は市場に織り込まれ済み**と考えられる。

**限界**: 最高20%でも77〜81%であり、ベース71%から+6〜9ptの改善にとどまる。単独で100%には届かない。

## 5.4 血統の複数切り口を階層モデルで統合する(`jrdb-pedigree-hierarchical.ts`)

**問題意識**: 「父馬×馬場」と「父馬×距離帯」を両方満たす馬に絞る方式(フィルタの重ね掛け)は、条件を掛け合わせるほどサンプルが分散して壊れる(5.2節で全パターン悪化)。

**正しい統合方法**: 各切り口を「全体平均からのズレ(効果量)」として足し合わせる階層モデル。
```
血統スコア = 全体平均 + 父馬の効果 + 父×馬場の効果 + 父×馬場状態の効果 + 父×距離帯の効果
```
各層は「上位層で説明できなかった残差」に対して経験ベイズ縮小をかけるため、データが薄い組み合わせは自動的に上位層へ寄る。サンプルを分散させずに多切り口を統合できる。

| モデル | 最高20%の回収率 | 最低との差 |
|---|---|---|
| 父馬のみ(単層) | 77.4% | 9.8pt |
| + 父×馬場 | 78.1% | 12.4pt |
| **+ 馬場状態 + 距離帯(4層)** | **82.1%** | **16.6pt** |
| 全8層(性別・クラス・馬齢・母父×馬場も追加) | 77.8% | 12.0pt(**悪化**) |

**4層が最良**。性別・クラス・馬齢・母父×馬場を足すと悪化する(ノイズを持ち込むだけ)。4層モデルで上位10%まで絞ると**85.2%**。ただし上位1〜2%まで絞っても82〜83%で頭打ちになり、**血統だけでは約85%が天井**。

この検証のためUKCに生年月日・毛色・馬記号・生産者・産地・父母の生年を追加解析した(馬齢=早熟/晩成の分析用)。結果的に馬齢は効かなかったが、フィールド自体は利用可能。

## 5.5 血統とエッジの組み合わせ(`jrdb-pedigree-plus-edge.ts`)

5.2節では勝率ベースの血統フィルタをエッジに重ねて全て悪化したが、**回収率ベースの血統(4層モデル)なら上乗せが出るか**を検証した。

| | フィルタなし | 血統上位30% | 血統上位10% |
|---|---|---|---|
| エッジ+16pt | 87.0%(2,505) | 89.9%(745) | 95.5%(249) |
| エッジ+20pt | 88.0%(1,254) | **91.4%**(382) | 96.9%(129) |

**上位10%の95〜97%は採用できない**。fold別で61%〜115%と極端に振れ、1foldあたり19〜76件しかない。

**採用できるのは「エッジ+20pt × 血統上位30% = 91.4%(試行382件)」**。fold別が88% / 92% / 89% / 95%と全て88%以上に収まり、一貫性がある。

| 戦略 | 回収率 | 最悪fold |
|---|---|---|
| エッジ+20pt単独 | 88.0% | 80% |
| 血統上位10%単独 | 85.2% | — |
| **エッジ+20pt × 血統上位30%** | **91.4%** | **88%** |

**組み合わせは平均を上げるだけでなく最悪ケースも改善している(80%→88%)**。これは2つが独立した情報を持っている証拠。勝率ベースでは失敗したが回収率ベースでは成功した点が重要。

**血統追求の結論**: 血統は回収率で測れば本物のシグナルであり、エッジと組み合わせて**87.4%→91.4%**まで改善する。ただし100%には届かない。現時点の最良戦略。


## 5.6 「固いレース」に絞る検証と独自の固さ指標(`jrdb-solid-races.ts`, `jrdb-solidity-score.ts`)

土台(エッジ+20pt × 血統上位30% = 91.4%)にレース単位の「固さ」条件を重ねた。

**既存の固さ定義**:
| 条件 | 試行 | 回収率 | fold別 |
|---|---|---|---|
| 条件なし | 382 | 91.4% | 88/92/89/95% |
| 市場gap>=15pt | 101 | 64.1% | 悪化 |
| 市場gap>=20pt | 68 | 60.3% | 悪化 |
| **JRDB指数gap>=12pt(堅い)** | 313 | **98.9%** | 115/103/85/97% |

**市場の確信度は逆効果、JRDB指数の確信度は大幅に効く**という非対称性が出た。市場が1頭を強く推すレースで我々がエッジを見出す馬は「その人気馬以外」になりがちで勝ち目が薄い。逆に**我々の指数が強く推していて市場が同意していない**馬が本命になる。98.9%は本セッション最高値だがfold変動30pt(85〜115%)で未確定。

**独自の固さ指標(5種)は全て不採用**: 集中度(オッズ分布のハーフィンダール指数)・階層の深さ(指数1位と3位の差)・市場の落ち着き(前日→最終のオッズ変動平均)・頭数・指数と市場の順位相関。いずれも5分位で単調にならず、4分位目が最悪・5分位目が最高という不自然な形。ただし全指標で共通して**「固い側より荒れそうな側の方が回収率が高い」**傾向が出た(単調でないため確定ではないが、「市場が迷うレースほど市場の誤りが大きい」という戦略の本質と整合する)。

**血統モデルの学習対象による過学習**: 血統モデルを「エッジ条件を満たす馬だけ」で学習させると、集計値は98.1%まで上がるがfold別が144/87/76/98%(変動68pt)と発散し、絞り込み幅に対しても非単調(87.0→91.3→98.1→90.6→98.3)。**全馬で学習した方が安定する**(91.4%、fold変動7pt)。学習データを絞ると過学習する典型例。

**現時点で自信を持って推奨できる戦略**:
| 戦略 | 回収率 | fold別 | 変動幅 | 試行 |
|---|---|---|---|---|
| エッジ+20pt単独 | 88.0% | 88/80/91/92 | 12pt | 1,254 |
| **エッジ+20pt × 血統上位30%(全馬学習)** | **91.4%** | **88/92/89/95** | **7pt** | 382 |
| エッジ+20pt × 血統30% × 指数gap>=12 | 98.9% | 115/103/85/97 | 30pt | 313(未確定) |


## 5.7 複勝で測り直して真の実力を確定させる(`jrdb-fukusho-combined.ts`)

**狙い**: 単勝版の最良(エッジ+20pt × 血統上位30% × 指数gap>=12 = 98.9%)は試行313件・的中率33%と少なく、fold変動が30pt(85〜115%)あり100%超えを確定できなかった。複勝は的中率が約2倍・試行が8倍になるため、**測定精度が格段に高い**。同じ3要素を複勝に適用して真の水準を測った。

複勝特有の扱い: JRAルールで8頭以上は3着まで的中(7頭以下は判定基準が変わるため除外)。市場の複勝確率は逆数の合計が約3になるため合計3に正規化。血統モデルも複勝の払戻で学習。

| エッジ閾値 | 最良の組み合わせ(血統上位30% + 指数gap>=12) | fold別 |
|---|---|---|
| +10pt | 88.8%(試行4,993) | 84/87/93/90% |
| +15pt | 90.0%(試行3,563) | 90/86/91/93% |
| **+20pt** | **90.9%**(試行2,562) | **94/89/90/91%** |
| +25pt | 89.1%(試行1,762) | 91/89/88/88% |
| +30pt | 90.1%(試行1,069) | 97/87/92/86% |

**全閾値・全foldが88〜97%に収まる**。変動幅は最良条件で5ptしかなく、本セッションで最も安定した結果。

積み上げ効果も明確(エッジ+20ptの場合):
| 段階 | 回収率 | 的中率 |
|---|---|---|
| エッジのみ | 85.4% | 55.4% |
| + 血統上位30% | 88.5% | 57.2% |
| + 指数gap>=12 | **90.9%** | **65.6%** |

血統(+3.1pt)と確信度(+2.4pt)がそれぞれ独立して効いており、単勝版で見えた構造が大きなサンプルで裏付けられた。

**結論: 単勝版の98.9%は上振れだった。我々の戦略の真の実力は約90%。** 複勝という精密な物差し(的中率66%・試行2,562件)が一貫して90%前後を示しており、これが実力の確定値。控除率20%に対して**約半分を取り戻した**水準であり、残り10ptが市場効率の壁。

## 6. 分析・学習スクリプト (`scripts/`)

| スクリプト | 内容 |
|---|---|
| `jrdb-bulk-download.ts` | 指定日数・種別のJRDBデータを再開可能な形で一括取得(`--days` `--types`) |
| `jrdb-calibration-check.ts` | 予測勝率をビン分けし実際の勝率と比較(温度パラメータ引数で指定可) |
| `jrdb-value-bet-experiment.ts` | 単勝の市場オッズ乖離(エッジ)閾値ごとの回収率比較 |
| `jrdb-fukusho-value-experiment.ts` | 複勝版の同上 |
| `jrdb-ev-experiment.ts` | EV(推定勝率×オッズ)閾値ごとの回収率比較 |
| `jrdb-sanrentan-value-experiment.ts` | 単勝エッジで選んだ馬だけで三連単ボックスを組む戦略の検証 |
| `jrdb-waku-venue-bias.ts` | 枠番・競馬場ごとの市場効率(全頭買い回収率)比較 |
| `jrdb-score-experiment.ts` | 総合指数の代わりに複数指数を再合成したスコア候補の比較 |
| `jrdb-train-model.ts` | **ロジスティック回帰モデルの学習・評価**。特徴量=JRDB指数群+市場オッズ+枠番+競馬場(one-hot)+血統(父馬・母父馬の経験ベイズ補正つき勝率エンコーディング、UKCと結合)。時系列でtrain(古い80%)/test(新しい20%)に分割し、test側の実回収率で評価。血統の勝率テーブルはtrainのみから算出しリークを防止。`--no-market`で市場オッズ由来特徴量を除外可能 |
| `jrdb-model-value-bet-experiment.ts` | 市場オッズを含まない学習済みモデルの確率を「自分の見立て」とし、市場確率とのエッジでバリューベットする戦略をhold-outテストデータで検証(参考として総合指数ベースの同戦略も並記) |
| `jrdb-kelly-experiment.ts` | バリューベットで選んだ馬にケリー基準で資金配分した場合の複利シミュレーション(フル/ハーフ/クォーターケリー×上限キャップ違いを比較) |
| `jrdb-value-bet-pedigree-experiment.ts` | 単勝バリューベットに父馬・母父馬の勝率フィルタを重ねた場合の効果を検証。単一train/testスプリットに加え、5分割ウォークフォワード検証(各foldは直前までの全データで学習)で一貫性も確認 |
| `jrdb-jockey-trainer-pedigree-experiment.ts` | 単勝バリューベットに騎手実勝率・調教師実勝率・父馬×馬場適性フィルタを重ねた場合の効果を、最初から5分割ウォークフォワード検証で確認(騎手・調教師は効果なし、父馬×馬場適性は有効性を確認) |
| `jrdb-distance-aptitude-experiment.ts` | 父馬×距離帯(スプリント/マイル/中距離/長距離)適性フィルタ、および父馬×馬場×距離帯を1キーに結合した場合の効果をウォークフォワード検証 |
| `jrdb-pedigree-combo-experiment.ts` | 父馬×馬場・父馬×距離帯という2つの独立フィルタを、別々に構築した上で積集合(両方満たす馬のみ)として組み合わせた場合の効果を検証(上乗せ効果なしと判明) |
| `jrdb-market-confidence-experiment.ts` | JRDBの総合指数(予測)ではなく市場オッズ自体の1位-2位差で「市場が固いと判断しているレース」を定義し、その絞り込みの強さ別に回収率を検証(高確信度帯ほど100%に接近するが、最上位帯はサンプル不足で未確定) |
| `jrdb-august-pedigree-forward-test.ts` | 血統(父馬×馬場適性)という1つの切り口だけに絞った分析ベースを直近月より前のデータで構築し、直近月(2026年8月)の実際のレース・結果にレース単位でぶつけるフォワードテスト機能 |
| `jrdb-keito-aptitude-experiment.ts` | UKCの父系統コード・母父系統コード(多世代の血統系譜を一族単位に分類したJRDB独自コード)を、父馬個体名の代わりに使った場合の効果をウォークフォワード検証(個体名ベースより不安定・信頼性低いと判明) |
| `jrdb-pedigree-depth-probe.ts` | 自己参照チェーンで祖先の個体名が何世代まで何%取れるかを実測し、インブリード検出の実現性を判定 |
| `jrdb-pedigree-tree-check.ts` | 実在の馬の系図を5世代まで展開して目視確認し、検出されたクロスを表示 |
| `jrdb-inbreeding-experiment.ts` | インブリード(クロスの有無・近交係数・濃いクロス)が回収率と相関するかを5分割で検証(逆相関と判明) |
| `jrdb-pedigree-only-monthly.ts` | JRDB指数も市場オッズも使わず血統だけで買い目を決めた場合の月次回収率(7月・8月)。学習は対象月より前のデータのみ |
| `jrdb-final-odds-value-bet.ts` | 前日オッズ(KYI)ではなく最終オッズ(TYB・発走15分前)でバリューベットのエッジを測り直し、オッズの動き自体の効果も検証(5.1節) |
| `jrdb-combined-strategy.ts` | 最終オッズ基準のエッジを土台に、血統・オッズ変動・万券指数・降級フラグ・ブリンカー等のフィルタを重ねた効果をウォークフォワード検証。引数でベースのエッジ閾値を指定(5.2節) |
| `jrdb-sire-roi-persistence.ts` | 血統を勝率ではなく回収率で評価し、その持続性(過去の高回収率が未来にも続くか)を7つの切り口で検証(5.3節) |
| `jrdb-pedigree-hierarchical.ts` | 血統の複数切り口を階層モデル(各層の効果量を経験ベイズ縮小して加算)で統合し、層を増やす効果を検証(5.4節) |
| `jrdb-fukusho-combined.ts` | 単勝で構築した最良戦略(エッジ×血統×指数確信度)を複勝に適用し、的中率の高さを利用して真の水準を高精度に測定(5.7節) |\n| `jrdb-solid-races.ts` | 市場の確信度(最終オッズの1-2番人気差)とJRDB指数の確信度で「固いレース」に絞った効果を検証(5.6節) |\n| `jrdb-solidity-score.ts` | 独自の固さ指標5種(集中度・階層の深さ・市場の落ち着き・頭数・指数と市場の一致度)を検証。血統モデルの学習対象による過学習も確認(5.6節) |\n| `jrdb-pedigree-plus-edge.ts` | 回収率ベースの血統4層モデルと最終オッズのエッジを組み合わせた効果を、fold別の一貫性つきで検証(5.5節) |

いずれも`npx tsx scripts/xxx.ts`で実行、`.env`はdotenv importで自動読込。

## 7. API仕様

### 7.1 JRDB系

| エンドポイント | 内容 |
|---|---|
| `GET /api/jrdb/races?date=` | 指定日の開催場・レース番号一覧(確信度ラベルつき) |
| `GET /api/jrdb/race?date=&venue=&raceNumber=` | 出走馬・分析・推奨買い目・(確定済みなら)結果と実績回収率 |
| `GET /api/jrdb/recent-picks?limit=` | 確度上位N件のレース(買い目つき) |
| `GET /api/jrdb/stats?daysBack=&confidence=` | 券種別的中率・回収率(期間・確信度フィルタ) |
| `GET /api/jrdb/stats/timeseries?granularity=day\|month&confidence=` | 日別/月別回収率推移・累積回収率 |
| `GET /api/jrdb/races/search?daysBack=&confidence=&venue=` | 検索可能なレース一覧(予測履歴ページ用) |
| `POST /api/jrdb/sync` → `GET /api/jrdb/sync/:jobId` | 未取得分の同期ジョブ開始・進捗取得 |

### 7.2 netkeiba系(既存)

`GET /api/predict/:raceId`、`POST /api/results/:raceId`、`GET /api/history`、`GET /api/stats`、
`GET /api/dashboard/recent-picks`、`POST /api/batch-predict` → `GET /api/batch-predict/:jobId`、
`POST /api/backfill` → `GET /api/backfill/:jobId`。詳細はコード参照(`server/index.ts`)。

## 8. netkeiba予想ロジック (predict.ts / probability.ts)

近走成績・距離/馬場/馬場状態適性・クラス適性・騎手相性・コンディション・血統(著名種牡馬の簡易リスト)・
市場評価の重み付き合計スコアをsoftmax(温度10、未キャリブレーション)で勝率化し、Harvilleモデルで
多通り馬券の的中確率を算出。買い目は確信度(1位2位の勝率差)に応じたBOXサイズで生成し、券種ごと確率上位3点、
予算配分ロジック(3,000円を券種均等→確率比例、100円単位)で金額を付与。詳細は変更なし(以前の版を参照)。

## 9. DB永続化 (db.ts)

SQLite(`better-sqlite3`、`data/predictions.db`)。`races`/`predictions`/`bets`/`horse_cache`の4テーブル。
netkeiba予想の保存・結果照合・券種別集計は従来通り(`getStats`/`getStatsByPeriod`は金額ベースの回収率に
対応済みだが、現在フロントエンドはJRDB側の同名エンドポイントを使用しており未呼び出し)。

## 10. 開発・実行方法

```bash
npm install
npm run dev:all     # フロントエンド(5173) + APIサーバー(3001) を同時起動
```

JRDBデータを使うには`.env`に`JRDB_USER`/`JRDB_PASSWORD`を設定し、`lhasa`(LZH解凍)をシステムに
インストールしておくこと(`sudo apt-get install lhasa`)。

個別起動: `npm run dev`（フロントのみ）/ `npm run server`（APIのみ、`nodemon --legacy-watch`で自動再起動）

ビルド: `npm run build`（`tsc -b && vite build`）

## 11. 既知の制約・未実装事項

- netkeiba経由の血統評価は簡易リストベース。JRDBはUKC(父馬・母父馬名+系統コード)を血統情報として使用
- JRDBの確率モデルはsoftmax(温度8)による簡易キャリブレーションのみ。真の意味での確率校正(アイソトニック回帰等)は未実施
- 三連単等の高分散券種はサンプルが薄い条件では過大評価されやすい(5節参照)。閾値の高い戦略は要・大規模検証
- ロジスティック回帰モデル(`jrdb-train-model.ts`)は実験段階で、本番の推奨買い目には未反映
- JRDBの配当データ(HJC)は結果確定当日に配信されるため、未確定レースの実績回収率は`null`
- netkeiba側の機能(結果確定・バッチ予想)は手動トリガー、状態はメモリ上のみで再起動により失われる

## 12. 今後の拡張候補

- ロジスティック回帰モデルの本番反映(検証が十分な結果を示した場合)
- バリューベット(市場オッズ乖離)戦略のUI組み込み・実運用
- データ範囲の拡張(現在数年 → 5-10年)による統計的信頼性の向上
- 血統をより本格的に活用(系統コード単位の集計、netkeibaの3世代血統とのクロス活用)
- 騎手・調教師単位の市場バイアス検証(まだ未着手)
- アイソトニック回帰等によるより厳密な確率キャリブレーション
